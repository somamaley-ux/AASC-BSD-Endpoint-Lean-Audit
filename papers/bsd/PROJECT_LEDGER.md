# Project ledger - BSD Endpoint Closure, Lean audit snapshot

## Proof surface

The stabilized proof surface is:

1. `BSDRankEndpointUnderAudit` binds each elliptic curve instance to official endpoint use.
2. Standard BSD carrier data instantiate the fixed adequate carrier.
3. Rank mismatch is equivalent to standard BSD mismatch normal form.
4. Standard mismatch normal form is equivalent to rank bridge-image exclusion.
5. Endpoint-used bridge-image exclusion induces theorem-level rank-status discrimination.
6. Endpoint-status bivalence exhausts governed positive/separator occupation.
7. Negative governed endpoint use has separator status and enters the
   bridge-image separator branch.
8. Endpoint governance is independent rank discrimination after legitimate roles are exhausted.
9. The imported kernel excludes independent same-domain BSD rank discriminators.
10. Rank mismatch is excluded, and rank equality follows.
11. The fixed-carrier rank equality matches the official BSD rank endpoint.
12. The refined formula layer is closed conditionally on formula-factor standing,
    with the guard aligned to `refinedBSDEndpoint_context_iff`.

## Lean audit additions

- Added standard BSD carrier instantiation theorem.
- Added semantic-not-computable endpoint-role clarification.
- Added official endpoint correspondence bridge.
- Added ordinary-counterexample versus endpoint-governing mismatch audit fork.
- Tightened theorem-level discriminator route to endpoint-used bridge-image exclusion.
- Updated denial burden and theorem ladder.
- Updated Lean-facing formalization map with the current bivalence,
  negative-governed-use, and separator-branch theorem names.
- Updated the refined formula handoff names to match the current Lean audit
  surface: `BSDFormula`, `BSDRefinedFormulaConditionalContext`, and
  `refinedBSDEndpoint_context_iff`.
- Added a Lean truth-boundary ledger distinguishing closed proof-spine claims,
  semantic carrier abstraction, external formula-factor standing, and the PDF
  snapshot boundary.
- Added aggregate parse checking for the pre-Lean manuscript signature map,
  outside the active axiom-free proof surface.
- Added an aggregate import-boundary guard verifying that the pre-Lean
  manuscript signature map is not imported by the active Lean audit surface.
- Strengthened the BSD status ledger with populated obligation-title,
  Lean-anchor, source-evidence, current-status, and progress-summary checks.
- Strengthened the truth-boundary ledger with item-catalog, row-count,
  Lean-anchor, explanatory-text, metadata-completion, and count-completion
  checks.

## Verification

Bundled PDF snapshot: retained from the manuscript handoff archive. It is a
snapshot artifact, not the authoritative current source render.

Current source note: `main.tex` has since received the Lean-name alignment for
the refined formula guard. The PDF should be regenerated in a LaTeX-enabled
environment before treating the bundled PDF as the latest source render.
